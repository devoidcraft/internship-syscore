# Excel to SQL Pipeline

Parallel pipeline to convert Excel files into SQL using schema-driven configuration.

## Setup

pip install -r requirements.txt

## Run

python main.py

## Config

- schema.json → table structure
- mapping.json → file → table mapping